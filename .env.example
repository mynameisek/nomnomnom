# =============================================================================
# nomnomnom — Environment Variables
# Copy to .env.local and fill in your values.
# Never commit .env.local — it is in .gitignore.
# =============================================================================

# Supabase — anon key (safe to expose client-side via NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase — service role key (SERVER ONLY — no NEXT_PUBLIC_ prefix)
# Source: Supabase Dashboard -> Project Settings -> API -> service_role (secret)
# Used by lib/supabase-admin.ts for LLM result writes. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# OpenAI — API key (SERVER ONLY — no NEXT_PUBLIC_ prefix)
# Source: https://platform.openai.com/api-keys
# Used by lib/openai.ts for menu parsing. Never expose to the browser.
OPENAI_API_KEY=your-openai-api-key-here

# Translation providers (all optional — cascade in order, fallback to LLM)
DEEPL_API_KEY=               # DeepL Free API key (500k chars/month free)
GOOGLE_TRANSLATE_API_KEY=    # Google Cloud Translation v2 key (500k chars/month free)
AZURE_TRANSLATOR_KEY=        # Azure Translator key (2M chars/month free)
AZURE_TRANSLATOR_REGION=     # Azure region (e.g. "westeurope")

# Admin — secret password for /admin panel (SERVER ONLY — no NEXT_PUBLIC_ prefix)
# Choose any strong password. A SHA-256 hash is stored in the cookie, not the raw value.
# The /admin page shows a password form when this is not set or the cookie is absent.
ADMIN_SECRET=your-admin-secret-here
