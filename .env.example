# =============================================================================
# nomnomnom — Environment Variables
# Copy to .env.local and fill in your values.
# Never commit .env.local — it is in .gitignore.
# =============================================================================

# Supabase — anon key (safe to expose client-side via NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase — service role key (SERVER ONLY — no NEXT_PUBLIC_ prefix)
# Source: Supabase Dashboard -> Project Settings -> API -> service_role (secret)
# Used by lib/supabase-admin.ts for LLM result writes. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# OpenAI — API key (SERVER ONLY — no NEXT_PUBLIC_ prefix)
# Source: https://platform.openai.com/api-keys
# Used by lib/openai.ts for menu parsing. Never expose to the browser.
OPENAI_API_KEY=your-openai-api-key-here
