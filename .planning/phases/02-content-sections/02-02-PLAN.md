---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/sections/DishCarousel.tsx
  - components/sections/Features.tsx
  - components/sections/ReverseSearch.tsx
  - components/sections/Social.tsx
  - components/sections/Pricing.tsx
  - components/sections/Faq.tsx
  - components/sections/FinalCta.tsx
  - app/page.tsx
autonomous: false
requirements:
  - CAROUSEL-01
  - CAROUSEL-02
  - CAROUSEL-03
  - FEAT-01
  - FEAT-02
  - REVERSE-01
  - REVERSE-02
  - REVERSE-03
  - SOCIAL-01
  - SOCIAL-02
  - SOCIAL-03
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - FAQ-01
  - FAQ-02
  - WAIT-07
  - WAIT-08

must_haves:
  truths:
    - "Scrolling below the hero shows sections in order: carousel, features, reverse search, social, pricing, FAQ, final CTA"
    - "Dish carousel auto-scrolls through 6 world dishes; clicking a card makes it active with orange border and scale-up"
    - "Features section shows 6 cards in a responsive grid with icons, titles, and descriptions"
    - "Reverse search demo types 'boulettes sauce yaourt turquie' then shows 3 match results with percentages and flags"
    - "Social section shows 4 feature cards + animated Taste Profile demo with bar chart and 'Opt-in uniquement' pill"
    - "Pricing shows 3 tiers (Gratuit/Pass/Pro) with Pass highlighted as Recommande"
    - "FAQ accordion expands/collapses 6 questions with smooth animation"
    - "Final CTA section shows 'Plus jamais hesiter devant un menu' with waitlist button"
    - "Background food collage visible behind the final CTA area"
  artifacts:
    - path: "components/sections/DishCarousel.tsx"
      provides: "Auto-scrolling dish carousel with active card state"
      contains: "DishCarousel"
    - path: "components/sections/Features.tsx"
      provides: "6 feature cards in responsive grid"
      contains: "Features"
    - path: "components/sections/ReverseSearch.tsx"
      provides: "Typing animation demo with match results"
      contains: "ReverseSearch"
    - path: "components/sections/Social.tsx"
      provides: "Social feature cards + TasteProfileDemo"
      contains: "Social"
    - path: "components/sections/Pricing.tsx"
      provides: "3 pricing tier cards"
      contains: "Pricing"
    - path: "components/sections/Faq.tsx"
      provides: "Accordion FAQ with 6 questions"
      contains: "Faq"
    - path: "components/sections/FinalCta.tsx"
      provides: "Final CTA with food collage background"
      contains: "FinalCta"
    - path: "app/page.tsx"
      provides: "Full page composition with all sections in order"
      contains: "FinalCta"
  key_links:
    - from: "components/sections/DishCarousel.tsx"
      to: "lib/data.ts"
      via: "DISHES and FOOD imports"
      pattern: "import.*DISHES.*from.*data"
    - from: "components/sections/ReverseSearch.tsx"
      to: "components/ui/FoodImage.tsx"
      via: "FoodImage for result thumbnails and food grid"
      pattern: "import.*FoodImage"
    - from: "components/sections/Faq.tsx"
      to: "lib/data.ts"
      via: "FAQS array import"
      pattern: "import.*FAQS.*from.*data"
    - from: "app/page.tsx"
      to: "all section components"
      via: "import and render in order"
      pattern: "import.*(DishCarousel|Features|ReverseSearch|Social|Pricing|Faq|FinalCta)"
---

<objective>
Build all remaining content sections below the hero: dish carousel, features grid, reverse search demo, social/gamification, pricing, FAQ, and final CTA — completing every visible section of the NOM landing page.

Purpose: These sections tell the full product story, from dish discovery to social features to pricing, culminating in a final conversion CTA. Together with the hero (Plan 01), they form the complete above-fold-to-footer experience.

Output: 7 section components wired into app/page.tsx, rendering the full landing page from top to bottom.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@/Users/ekitcho/Downloads/nom-landing-v5.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DishCarousel, Features, and ReverseSearch sections</name>
  <files>
    components/sections/DishCarousel.tsx
    components/sections/Features.tsx
    components/sections/ReverseSearch.tsx
  </files>
  <action>
**components/sections/DishCarousel.tsx** — `"use client"` component. Port from v5 with Tailwind.

- Section wrapper with `py-8 pb-12`. Section label: "Explorer les plats du monde" centered, uppercase, letter-spacing-[3px], text-[10px], text-white/15, font-bold, mb-3.
- `useState(0)` for active index. `useEffect` with `setInterval(() => setActive(p => (p+1) % DISHES.length), 3200)`. Clean up interval on unmount.
- Second `useEffect` watching `active` — scrolls ref to `active * 218` with smooth behavior.
- Scroll container: `useRef<HTMLDivElement>`, flex row, gap-3, overflow-x-auto, snap-x snap-mandatory, px-6, `no-scrollbar` utility class (from globals.css), pb-2.
- Each card: `min-w-[206px] max-w-[206px] flex-shrink-0 snap-center rounded-2xl overflow-hidden cursor-pointer`. Active card: `border-brand-orange/40 scale-[1.03] opacity-100`. Inactive: `border-white/[0.04] scale-95 opacity-40`. All with `border transition-all duration-400`.
- Card content (matching v5 exactly):
  - Image area: h-[110px] relative overflow-hidden. Gradient background from FOOD[d.img]. `<img>` with Unsplash URL, object-cover. Gradient overlay (transparent 30% → brand-bg 95%). Price badge top-right (black/50 backdrop-blur, orange text). Spice indicators top-left (repeat chili emoji up to 3).
  - Text area: below image, padding 0 12px 12px, margin-top -22px, relative z-2. Country flag emoji (text-xl), dish original name (text-[15px] font-extrabold), translated name (text-[10px] text-white/40).
- `onClick` on each card sets active index.

**components/sections/Features.tsx** — Server Component (NO "use client"). Port from v5.

- `id="features"` on section element (this is the scroll target for "Comment ca marche" CTA).
- Section padding: py-12.
- Inner wrap: `mx-auto max-w-content px-6`.
- Section label: "Fonctionnalites" — use the same label style as v5 (text-[11px] text-brand-orange uppercase tracking-[2px] font-bold mb-2).
- Title: "Le menu devient une experience" — text-[clamp(24px,3.5vw,38px)] font-extrabold leading-tight tracking-tight mb-2.
- Subtitle: "Meme QR code que d'habitude — mais une interface 10x plus claire pour choisir." — text-sm text-white/40 max-w-[50ch] leading-relaxed mb-6.
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-2.5`.
- Each feature card: bg-white/[0.02] border border-white/5 rounded-2xl p-4 flex gap-3.5. Icon container: w-[42px] h-[42px] rounded-xl bg-brand-orange/[0.06] border border-brand-orange/10 flex items-center justify-center text-xl flex-shrink-0. Title: text-sm font-bold mb-0.5. Description: text-xs text-white/40 leading-relaxed.
- Import FEATURES from lib/data.ts.

**components/sections/ReverseSearch.tsx** — `"use client"` component. Port from v5.

- Section padding: py-12.
- Layout: flex wrap-reverse, gap-9, items-center, justify-center. On mobile, stacks with demo on top and text below.
- Left side (text + food grid, max-w-[380px]):
  - Label: "Recherche inversee" (same label style).
  - Title: "Tu te souviens du gout, pas du nom ?"
  - Subtitle: "Decris le plat par memoire — ingredients, texture, pays — et l'IA le retrouve parmi des milliers de plats du monde."
  - Food image grid below: 3-column grid, gap-1.5. Three FoodImage components at indexes 4, 7, 2 with `className="w-full h-[72px] rounded-xl"` (use img tag with object-cover, not the FoodImage component's fixed size — override with className).
- Right side (ReverseDemo widget, with animate-float + animation-delay 1s):
  - Container: bg-white/[0.03] border border-white/6 rounded-[22px] p-5 max-w-[370px] w-full.
  - "RECHERCHE INVERSEE" label at top.
  - Input display area: bg-white/[0.04] rounded-xl p-2.5 px-3, border transitions to orange when typing. Shows query text or placeholder "Decris un plat de memoire...". Blinking cursor `animate-blink` when typing.
  - Typing animation: useEffect with full loop (10500ms interval). Start delay 1400ms, then type "boulettes sauce yaourt turquie" at 48ms/char. After typing complete, 280ms delay then show results.
  - **Memory-safe cleanup:** Track all timeout and interval IDs. Clear ALL on unmount.
  - Results: 3 items (Yogurtlu Kofte 96%, Kibbeh bil Laban 74%, Koufteh Tabrizi 38%). Each with FoodImage (size 38), flag + name, match percentage badge (first result green, others muted), description. animate-fade-up with staggered delay (i*100ms).
  </action>
  <verify>
Run `npm run build` — clean build. Run dev server. Scroll below hero: carousel visible with auto-scrolling cards, features grid with 6 cards, reverse search with typing demo. Verify: clicking a carousel card activates it. Verify: reverse search types and shows results on loop.
  </verify>
  <done>
DishCarousel renders 6 world dish cards with auto-scroll (3.2s) and click-to-activate with orange border/scale. Features renders 6 cards in responsive grid with section id="features". ReverseSearch shows animated typing demo with 3 match results. All three sections build cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Social, Pricing, FAQ, FinalCta sections and wire full page</name>
  <files>
    components/sections/Social.tsx
    components/sections/Pricing.tsx
    components/sections/Faq.tsx
    components/sections/FinalCta.tsx
    app/page.tsx
  </files>
  <action>
**components/sections/Social.tsx** — `"use client"` component. Port from v5.

- `id="social"` on section. Padding: py-12.
- Header row: flex justify-between items-end flex-wrap gap-3.5 mb-6. Left side: label "Social & gamification", title "Le social est un mode, pas un mur", subtitle "Tout est optionnel. Le scan marche sans. Mais si tu veux jouer...". Right side: Pill with `glow` prop containing "Opt-in uniquement" (SOCIAL-03).
- Content: flex wrap gap-5 items-start.
- Left column (flex-1, min-w-[380px]): 4 BELI_FEATURES cards. Each card: bg-white/[0.02] border border-white/5 rounded-2xl p-4 flex gap-3.5. Icon box same as features. Title, description, tag pill (text-[9px] text-brand-orange/60 bg-brand-orange/[0.06] px-1.5 py-0.5 rounded-md font-semibold).
- Right column: TasteProfileDemo component (inline in this file or extracted — prefer inline for single-use).
  - TasteProfileDemo: bg-white/[0.03] border border-white/6 rounded-[22px] p-5 max-w-[320px] w-full, animate-float with delay-2000.
  - Header: "TASTE PROFILE" label + "@mariefoodie" name on left, "247" plats scannes on right.
  - Bar chart: 5 cuisines (Japonaise 34% orange, Turque 22% red, Italienne 18% green, Vietnamienne 14% #6366f1, Autre 12% white/15). Each row: label (72px right-aligned), progress bar (h-3.5 rounded-full, bg track at white/[0.04]), percentage text. Use `motion.div` from "motion/react" with `initial={{ width: 0 }} animate={{ width: \`${pct}%\` }}` and staggered delay for bar animation.
  - Spice/Budget/Style cards: 3 flex row, each with value emoji, subtitle text.
  - Match score teaser: bg-brand-orange/5 border border-brand-orange/10 rounded-xl p-2 px-3. "Match avec @lucas" left, "87%" green right.

**components/sections/Pricing.tsx** — Server Component (NO "use client"). Port from v5.

- `id="pricing"` on section. Padding: py-14.
- Header: centered text — label "Tarifs", title "Simple et transparent", subtitle "Scanner est gratuit. Tu ne payes que si tu veux aller plus loin."
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`.
- 3 pricing tiers (data inline — small, single-use):
  - Gratuit: 0E pour toujours, 3 features, "Commencer" ghost button
  - Pass: 9,99E achat unique a vie, 5 features, "Acheter le Pass" primary button, `pop: true` — gets orange gradient top bg, orange border, "Recommande" badge absolute top-right
  - Pro: 3,99E /mois, 5 features, "S'abonner" ghost button
- Each feature line: green checkmark box + feature text.
- Credits bonus box below grid: centered, bg-white/[0.02], border, rounded-xl, text about earning credits. Include the lightbulb emoji and bold "Credits bonus".
- Buttons use Btn component from components/ui/Btn.tsx — primary for Pass tier, secondary for others. Full width with rounded-xl.

**components/sections/Faq.tsx** — `"use client"` component. Port from v5 with motion/react upgrade.

- `id="faq"` on section. Padding: py-10 pb-12.
- Inner wrap: max-w-[680px] mx-auto px-6.
- Label "Questions frequentes", title "Tout ce que tu veux savoir", mb-4.
- Import FAQS from lib/data.ts.
- FaqItem sub-component (can be in same file): `useState(false)` for open state.
  - Container: bg-white/[0.02] border border-white/5 rounded-2xl mb-2 overflow-hidden.
  - Button: full width, px-5 py-4, flex justify-between items-center. Question text left, "+" icon right that rotates 45deg when open (use `motion.span` with `animate={{ rotate: open ? 45 : 0 }}`). Icon color: open ? brand-orange : white/20.
  - Answer: Use `AnimatePresence` + `motion.div` with `initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }}` for smooth accordion (NOT max-height hack). Transition 0.3s easeInOut. `overflow-hidden` on the motion.div.
  - Answer text: px-5 pb-4 text-sm text-white/45 leading-relaxed.

**components/sections/FinalCta.tsx** — Server Component (NO "use client"). Port from v5.

- Two parts combined in one component:

1. **Waitlist section** (`id="waitlist"`): padding py-9. Inner wrap. Rounded-3xl with overflow-hidden, relative, border border-brand-orange/10.
   - Background food collage: absolute inset-0, flex row, opacity-[0.08], overflow-hidden. 8 FOOD items each at flex-0-0-25% height-full. Each has gradient bg + img with object-cover. Use plain `<img>` with onError hide.
   - Content overlay (relative, p-8 px-6): flex justify-between items-center flex-wrap gap-4. Left: h3 "Rejoins les premiers testeurs" (text-xl font-extrabold), subtitle about beta access. Right: placeholder for email form (just the CTA button for now — form logic is Phase 3). Use Btn primary "C'est parti" — Phase 3 will wire Supabase.
   - **NOTE:** The email input + form submission is Phase 3 scope. For now render a static Btn "Rejoindre la liste d'attente" as a visual placeholder.

2. **Final CTA** section: py-[70px] px-6 pb-[90px] text-center, relative.
   - Radial gradient background glow: absolute div, 450x450, centered, radial-gradient from brand-orange/[0.04] to transparent, pointer-events-none.
   - h2: "Plus jamais hesiter" line 1, "devant un menu." line 2 in italic text-white/[0.18] font-normal.
   - Subtitle: "200 cuisines. 50 langues. Un seul geste." text-[15px] text-white/30 max-w-[40ch] mx-auto mb-7.
   - Btn primary big: "Rejoindre la liste d'attente" with `href="#waitlist"`.
   - Tiny footer text: "Gratuit pour scanner. Toujours." text-[10px] text-white/[0.12] mt-3.

**app/page.tsx** — Import ALL section components and render in order:
```
Hero → DishCarousel → Features → ReverseSearch → Social → Pricing → Faq → FinalCta
```
Keep page.tsx as a Server Component (no "use client" needed — all client components are imported, which is valid).

Import FOOD from lib/data.ts ONLY if needed directly in page.tsx (likely not — each section handles its own data imports).
  </action>
  <verify>
Run `npm run build` — clean build. Run dev server. Scroll entire page from top to bottom: Hero → Carousel → Features → Reverse Search → Social → Pricing → FAQ → Final CTA → Footer. Verify: (1) All sections visible in correct order, (2) FAQ accordion opens/closes smoothly, (3) Pricing Pass tier highlighted, (4) Final CTA has food collage background, (5) No console errors.
  </verify>
  <done>
All 7 remaining sections render correctly. Full page scrolls from hero to footer with all sections in order. FAQ accordion uses motion height:auto animation. Pricing Pass tier highlighted as Recommande. Social shows TasteProfileDemo with animated bars. Final CTA has food collage background and "Plus jamais hesiter" heading. Clean build.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete landing page visually</name>
  <files>app/page.tsx</files>
  <action>
  User visually verifies all landing page sections match the v5 reference design.

  What was built: Complete landing page — all content sections from hero to final CTA, with animations, interactions, and French copy matching the v5 reference design.

  How to verify:
  1. Visit http://localhost:3000 (npm run dev)
  2. Full scroll test: Scroll from top to bottom and verify section order: Hero → Dish Carousel → Features → Reverse Search → Social/Gamification → Pricing → FAQ → Final CTA → Footer
  3. Dish Carousel: Cards auto-scroll every 3.2s. Click a card — it should get an orange border and scale up. Other cards should fade and shrink.
  4. Features: 6 cards in a responsive grid with icons. Resize browser — should go from 3 columns to 2 to 1.
  5. Reverse Search: Watch the typing animation "boulettes sauce yaourt turquie" — 3 results appear with match percentages (96%, 74%, 38%). Should loop.
  6. Social: 4 feature cards on the left, Taste Profile demo on the right with animated bar chart. "Opt-in uniquement" pill visible.
  7. Pricing: 3 tiers. Middle "Pass" tier highlighted with orange border and "Recommande" badge. Credits bonus text below.
  8. FAQ: Click each question — smooth expand/collapse animation. "+" rotates to "x" when open.
  9. Final CTA: "Plus jamais hesiter devant un menu" heading. Food images visible at low opacity behind the waitlist section.
  10. CTA scrolling: Click "Comment ca marche" in hero — should smooth-scroll to Features. Click "Rejoindre la liste d'attente" — should scroll to waitlist section.
  11. Mobile: Resize to 375px width. All sections should stack and remain readable.
  </action>
  <verify>User confirms visual correctness of all sections</verify>
  <done>User types "approved" confirming all landing page sections look correct and interactions work</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 9 sections visible in correct order (hero through final CTA)
- Dish carousel auto-scrolls and supports click-to-activate
- Reverse search typing animation loops correctly
- FAQ accordion uses motion/react height:"auto" animation (not max-height)
- Pricing Pass tier visually highlighted
- CTA buttons scroll to correct section anchors
- Page responsive from 375px mobile to desktop widths
- No React console warnings about unmounted state updates
</verification>

<success_criteria>
- Every section from v5 reference is present and matches the visual target
- All interactive demos (carousel, reverse search, taste profile bars, FAQ) animate correctly
- French copy matches v5 exactly — no paraphrasing
- Page builds cleanly with correct Server/Client component split
- Mobile-first responsive layout works from 375px to desktop
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
