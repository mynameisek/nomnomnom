---
phase: 02-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/page.tsx
  - next.config.ts
  - lib/data.ts
  - components/ui/FoodImage.tsx
  - components/ui/Pill.tsx
  - components/ui/Btn.tsx
  - components/sections/Hero.tsx
  - components/sections/PhoneDemo.tsx
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05

must_haves:
  truths:
    - "Hero displays tagline 'Scanne le menu. Comprends chaque plat.' with gradient text on the second line"
    - "Three capability pills appear above the tagline (QR/Photo/Lien/Voix, Sans inscription, 50+ langues)"
    - "Primary CTA 'Rejoindre la liste d'attente' and secondary CTA 'Comment ca marche' are visible and scroll to correct anchors"
    - "Food photo mosaic with 5 images + '200+' badge and '200+ cuisines du monde' text appears below CTAs"
    - "Phone mockup auto-cycles through 4 phases: QR scan -> scan line animation -> menu with dish cards -> AI Top 3 panel"
    - "Phone demo floats with gentle animation alongside the hero text"
  artifacts:
    - path: "lib/data.ts"
      provides: "FOOD, DISHES, FEATURES, BELI_FEATURES, FAQS data arrays"
      contains: "FOOD"
    - path: "components/ui/FoodImage.tsx"
      provides: "Reusable food image with gradient+emoji fallback"
      contains: "FoodImage"
    - path: "components/ui/Pill.tsx"
      provides: "Reusable pill component"
      contains: "Pill"
    - path: "components/ui/Btn.tsx"
      provides: "Reusable button component with primary/secondary variants"
      contains: "Btn"
    - path: "components/sections/Hero.tsx"
      provides: "Hero section with tagline, pills, CTAs, food mosaic"
      contains: "Hero"
    - path: "components/sections/PhoneDemo.tsx"
      provides: "Animated phone mockup with 4-phase demo cycle"
      contains: "PhoneDemo"
    - path: "app/page.tsx"
      provides: "Page composition wiring Hero and PhoneDemo"
      contains: "Hero"
  key_links:
    - from: "components/sections/Hero.tsx"
      to: "#waitlist, #features"
      via: "anchor href scroll"
      pattern: "href.*#(waitlist|features)"
    - from: "components/sections/PhoneDemo.tsx"
      to: "lib/data.ts"
      via: "FOOD array import"
      pattern: "import.*FOOD.*from.*data"
    - from: "app/page.tsx"
      to: "components/sections/Hero.tsx"
      via: "component import"
      pattern: "import.*Hero"
---

<objective>
Build the hero section and animated phone demo — the first visible content users see on the NOM landing page.

Purpose: The hero is the single most important conversion element. It must communicate the value prop (scan any menu, understand every dish), show the product in action (phone demo), and provide clear CTAs.

Output: Hero section + phone demo rendering in app/page.tsx with all supporting UI components (Pill, Btn, FoodImage) and shared data constants.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@/Users/ekitcho/Downloads/nom-landing-v5.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared data, UI primitives, and global animation keyframes</name>
  <files>
    lib/data.ts
    components/ui/FoodImage.tsx
    components/ui/Pill.tsx
    components/ui/Btn.tsx
    app/globals.css
    next.config.ts
  </files>
  <action>
**lib/data.ts** — Extract ALL data arrays from v5 reference into a single shared module (no "use client" directive — plain data). Include:
- `FOOD` array (8 items with `url`, `grad`, `emoji` — exact Unsplash URLs from v5)
- `DISHES` array (6 items with `original`, `translated`, `country`, `img`, `price`, `spice`)
- `FEATURES` array (6 items with `icon`, `title`, `desc`)
- `BELI_FEATURES` array (4 items with `icon`, `title`, `desc`, `tag`)
- `FAQS` array (6 items with `q`, `a`)
- Export a type `FoodItem` for the FOOD array shape

Copy exact French text and Unsplash URLs from v5 — do NOT paraphrase.

**components/ui/FoodImage.tsx** — `"use client"` component. Props: `index: number`, `className?: string`, `size?: number`. Uses `useState` for load/error state. Renders a div with gradient background from `FOOD[index % FOOD.length]`, an `<img>` (plain HTML img, not next/image — for simplicity inside phone mockup and small sizes), and emoji fallback on error. Use Tailwind classes, not inline styles. The `size` prop sets both width and height via inline style (since sizes are dynamic). Border-radius via className.

**components/ui/Pill.tsx** — Server-safe component (no "use client"). Props: `children: React.ReactNode`, `glow?: boolean`. Renders a `<span>` with rounded-full, appropriate bg/border/text color based on `glow` prop. Use Tailwind classes matching v5 styling: glow variant gets `bg-brand-orange/[0.12] border-brand-orange/[0.22] text-brand-orange`, default gets `bg-white/[0.06] border-white/[0.07] text-white/50`. Font: text-[11px] font-bold whitespace-nowrap.

**components/ui/Btn.tsx** — Server-safe component. Props: `children`, `primary?: boolean`, `big?: boolean`, `className?: string`, plus standard button/anchor HTML props. Renders as `<a>` if `href` is provided, otherwise `<button>`. Primary variant: gradient bg from brand-orange to brand-red, white text, orange shadow. Secondary: bg-white/[0.04] border-white/10 text. Big variant: larger padding and font. Use Tailwind — replicate v5's gradient `linear-gradient(135deg, #ff6b35, #e8453c)` using `bg-gradient-to-br from-brand-orange to-brand-red`. Rounded-full (border-radius: 60px equivalent).

**app/globals.css** — Add inside the existing `@theme` block:
- `--animate-scan-line: scan-line 1.5s ease-in-out infinite;`
- `--animate-fade-up: fade-up 0.4s ease both;`
- `--animate-blink: blink 0.8s infinite;`
- `--animate-float: float 7s ease-in-out infinite;`

Add `@keyframes` blocks OUTSIDE `@theme` (at file root level):
- `scan-line`: translateY(-65px) → translateY(65px) → translateY(-65px)
- `fade-up`: opacity 0, translateY(12px) → opacity 1, translateY(0)
- `blink`: opacity 1 (0-50%) → opacity 0 (51-100%)
- `float`: translateY(0) → translateY(-8px) → translateY(0)

Add `@utility no-scrollbar` for carousel scrollbar hiding (used in Plan 02).

**next.config.ts** — Add `images.remotePatterns` for `images.unsplash.com` (protocol: "https", hostname: "images.unsplash.com", pathname: "/**"). Keep existing `turbopack.root`.

**IMPORTANT:** If `@keyframes` inside `@theme` causes Tailwind v4 build errors, place them outside `@theme` at the root CSS level. The `--animate-*` custom properties go in `@theme`, but `@keyframes` rules go outside.
  </action>
  <verify>
Run `npm run build` from the project root. Expect: clean build, no errors. Verify lib/data.ts has all 5 arrays with correct item counts (FOOD:8, DISHES:6, FEATURES:6, BELI_FEATURES:4, FAQS:6). Verify next.config.ts has images.remotePatterns entry. Verify globals.css has the 4 animation custom properties.
  </verify>
  <done>
lib/data.ts exports FOOD (8), DISHES (6), FEATURES (6), BELI_FEATURES (4), FAQS (6) arrays. FoodImage, Pill, Btn components exist in components/ui/. globals.css has scan-line, fade-up, blink, float keyframes. next.config.ts allows Unsplash images. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section and PhoneDemo components, wire into page</name>
  <files>
    components/sections/Hero.tsx
    components/sections/PhoneDemo.tsx
    app/page.tsx
  </files>
  <action>
**components/sections/PhoneDemo.tsx** — `"use client"` component. Faithfully port the PhoneDemo from v5 reference to Tailwind classes.

Implementation:
- `useState(0)` for phase (0-3), `useEffect` with setInterval(9000ms) loop
- Phase timing: setPhase(0) at start, setTimeout(1) at 1300ms, setTimeout(2) at 2800ms, setTimeout(3) at 4200ms
- **Memory-safe cleanup:** Store all timeout IDs in an array, clearInterval + forEach clearTimeout on unmount
- Phase 0-1 (scan view): Centered QR code area with 4x4 grid pattern. Phase >= 1 adds orange border glow and `animate-scan-line` scanning overlay + "Menu detecte..." text. Phase 0 shows "Scanne le QR du resto" in muted text.
- Phase 2+ (menu view): Resto header "Umai Ramen / Japonais · Neudorf / 4.3 rating", filter pills (Vege, Porc, Epice, <18E), three dish cards (Tori Paitan 15.80E, Shio Ramen 15.50E, Udon Karaage 16.00E) with FoodImage thumbnails at size 42, animate-fade-up entrance.
- Phase 3 (AI panel): Dashed-border AI suggestion box with italic user query "consistant, chaud, poulet, pas epice" and Top 3 results list, animate-fade-up entrance.
- Phone shell: 280px width, dark bg, rounded-[36px], 2.5px border white/10, inner notch (76x22px centered top), inner screen area rounded-[29px] h-[540px] with overflow hidden.
- Use exact French copy from v5: dish names, prices, descriptions, tags.

**components/sections/Hero.tsx** — `"use client"` component (needs scroll handler for CTAs).

Layout: CSS grid — `grid-cols-1 lg:grid-cols-[1.15fr_0.85fr]` with gap-8. Mobile stacks vertically (text first, phone second).

Left column:
1. Pill row: three Pill components with exact v5 text — first with `glow` prop
2. h1 tagline: "Scanne le menu." on line 1, "Comprends chaque plat." on line 2 with gradient text (`bg-gradient-to-br from-brand-orange to-brand-red bg-clip-text text-transparent`). Font size: `style={{ fontSize: "clamp(34px, 4.5vw, 56px)" }}` for exact v5 sizing. font-black (900 weight), leading-none, tracking-tighter.
3. Subtitle paragraph: exact v5 text with `<b>NOM</b>` and `<b>Top 3</b>` in slightly brighter white
4. CTA row: Btn primary big "Rejoindre la liste d'attente" with `href="#waitlist"`, Btn big "Comment ca marche ↓" with `href="#features"`
5. Food mosaic: 5 FoodImage at size 48 (indexes 0,5,3,6,2) + a "200+" badge div + "Plus de 200 cuisines du monde couvertes" text below

Right column:
- PhoneDemo wrapped in `animate-float` div, centered with `flex justify-center`
- On mobile (below lg), the phone should appear below the hero text

Section padding: `pt-[90px] pb-4` (accounts for fixed nav).

**app/page.tsx** — Import Hero. Render `<Hero />` as the first and only section (more sections added in Plan 02). Keep as Server Component — Hero is a client component imported into it.

Use `max-w-content` and `px-6` for the wrap container inside Hero (matching Phase 1 pattern from Nav).
  </action>
  <verify>
Run `npm run build` — clean build. Run `npm run dev` and visit localhost:3000. Verify: (1) Hero section visible with pills, tagline with gradient, CTAs, food mosaic. (2) Phone mockup visible, auto-cycling through 4 phases. (3) Mobile responsive — stack vertically below lg breakpoint. (4) No console errors about unmounted state updates.
  </verify>
  <done>
Hero section renders with gradient tagline, 3 pills, 2 CTAs, food mosaic with 5 images + "200+" badge. PhoneDemo auto-cycles through scan/detect/menu/AI phases on a 9s loop. Page builds cleanly and is responsive. CTAs link to #waitlist and #features anchors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify hero and phone demo visually</name>
  <files>app/page.tsx</files>
  <action>
  User visually verifies the hero section and phone demo match the v5 reference design.

  What was built: Hero section with animated phone demo — the top-of-page experience for the NOM landing page. Includes gradient tagline, capability pills, CTAs, food mosaic, and a phone mockup that cycles through scan → menu → AI Top 3.

  How to verify:
  1. Visit the local dev server (npm run dev → http://localhost:3000)
  2. Hero text: Verify "Scanne le menu." + gradient "Comprends chaque plat." tagline is visible
  3. Pills: Three pills above the tagline — first one orange-tinted with camera icon
  4. CTAs: "Rejoindre la liste d'attente" (orange gradient) and "Comment ca marche" (ghost) buttons visible
  5. Food mosaic: 5 small food images + "200+" orange badge below the CTAs
  6. Phone demo: Watch the phone mockup cycle through: QR code → scanning line → menu with dishes → AI suggestion panel (full cycle ~9 seconds)
  7. Mobile: Resize browser to mobile width — hero text should stack above the phone demo
  8. No errors: Check browser console for React errors
  </action>
  <verify>User confirms visual correctness</verify>
  <done>User types "approved" confirming hero and phone demo look correct</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 5 data arrays in lib/data.ts match v5 item counts
- Hero section renders correctly at desktop and mobile widths
- Phone demo cycles through 4 phases without errors
- CTA buttons have correct href anchors (#waitlist, #features)
- Food images load from Unsplash with gradient+emoji fallback on error
</verification>

<success_criteria>
- Hero visible with gradient tagline, pills, CTAs, and food mosaic
- Phone demo auto-animates through scan → detect → menu → AI on 9s cycle
- Page responsive: grid layout on desktop, stacked on mobile
- Clean build with no TypeScript or console errors
- Shared data and UI components ready for Plan 02 consumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-01-SUMMARY.md`
</output>
